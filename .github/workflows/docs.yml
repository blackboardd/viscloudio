on:
  push:
    branches:
      - main
      - 'releases/**'

jobs:
  docs:
    needs: build

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: generate html files for sphinx
        run: |
          ./sphinx_make.bat html
